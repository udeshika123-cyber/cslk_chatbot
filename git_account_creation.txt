4/23/2025
git init
git clone https://github.com/udeshika123-cyber/prabhashi21.git
git branch         = Check the branch
git checkout -b new-branch-name   = Create a New Branch
git push origin new-branch-name    =  To up to date(Push the Branch to GitHub)
git checkout branch-name               = Switch Between Branches
git status                                          
git add<filename>/(.)
git commit -m "Add a description of what you changed"
git pull origin main
Set the remote origin               = git remote add origin https://github.com/UDESHIKA721/udeshika.git
git push origin numbers
git branch -M main            = to get the main branch

when a push error
git pull --rebase origin numbers
git add numbers.java
git commit -m "Add even/odd number checker program"
git push origin numbers

Now you should finally see the green button:
Compare & pull request
Click it, add your title and description, and then hit “Create pull request” ✅

Delete the broken numbers branch locally
git checkout main
git branch -D numbers

Recreate the branch from main
git checkout -b numbers

Remove the Existing Remote (if necessary)
git remote remove origin
